# Kculture 콘텐츠·퀘스트 구현 설명

이 폴더는 기존 `gogumi3/kculture` 프로젝트 전체를 기준으로 작업한 결과다. 별도의 `src` 복사 작업 없이 이 폴더 자체를 IntelliJ에서 열면 된다.

## 기존 구조 보존

기존 `user`, `travel`, `recommendation`과 모든 domain 엔터티는 유지했다. content와 quest 기능을 완성하기 위해 필요한 파일만 추가·수정했다. `build.gradle`, Flyway `V1__init.sql`, `application.properties`는 수정하지 않았다.

## 완성한 content 기능

- 기존 곡 전체 조회, ID 조회, 제목 검색 유지
- 존재하지 않는 곡 조회 시 404 처리
- 공백 검색어 400 처리
- 곡별 MV 분석 작업 생성 및 분석 이력 조회
- 분석 상태 `PENDING → RUNNING → DONE/FAILED` 변경
- 분석 완료 시 한국적 요소 목록 저장
- 분석 결과를 영상 등장 시간순으로 조회
- 대상/필드/언어별 번역 저장 및 조회
- 같은 번역 키가 다시 들어오면 새 행을 만들지 않고 기존 번역문 갱신

content에 추가한 하위 패키지는 다음과 같다.

- `controller`: HTTP 요청을 받아 Service에 전달하고 JSON 응답 반환
- `dto`: 요청과 응답 데이터 구조. Entity 자체를 프론트에 직접 보내지 않음
- `exception`: content API의 400/404 오류 응답 처리
- `repository`: 분석, 문화 요소, 번역 데이터의 DB 조회
- `service`: 분석 상태 규칙, 요소 저장, 번역 저장 같은 실제 업무 처리

`MvAnalysis.start()`와 `finish()`는 상태 변경과 시각 기록을 엔터티 안에서 함께 처리하기 위해 추가했다. `Translation.updateText()`는 중복 번역 행을 생성하지 않고 기존 문장을 수정하기 위해 추가했다.

## 완성한 quest 기능

- 활성 퀘스트 전체 조회
- 퀘스트 상세 및 단계·미션 조회
- 퀘스트와 여러 단계·미션 생성
- 추천 세션에서 사용자가 선택한 장소를 실제 퀘스트로 변환
- 사용자별 퀘스트 시작과 이어하기
- 첫 단계만 `UNLOCKED`, 나머지는 `LOCKED` 처리
- GPS 좌표를 이용한 도착 거리 계산
- 퀴즈 정답 확인 또는 사진 주소 확인
- 미션 완료 시 스탬프 저장
- 현재 단계 `DONE` 처리 후 다음 단계 `UNLOCKED`
- 마지막 단계 완료 시 전체 퀘스트 `COMPLETED`
- 사용자별 진행 기록 및 스탬프 조회
- 동일 완료 요청이 다시 들어와도 스탬프 중복 저장 방지

quest에 추가한 하위 패키지는 다음과 같다.

- `controller`: 퀘스트 조회·생성·시작·도착·완료 URL
- `dto`: 퀘스트, 단계, 진행 상태, GPS, 미션, 스탬프 요청/응답
- `exception`: 퀘스트 데이터 없음과 잘못된 진행 요청 처리
- `repository`: Quest, QuestStep, Mission, Progress, StepStatus, Stamp 조회
- `service`: 퀘스트 생성과 사용자 진행 규칙 처리

`UserQuestProgress.moveTo()`와 `complete()`는 현재 단계 변경과 전체 완료 처리를 담당한다. `UserStepStatus.unlock()`과 `complete()`는 잠금 상태가 잘못된 순서로 바뀌는 것을 막는다.

`GeoDistance`는 하버사인 계산식으로 사용자 좌표와 장소 좌표 사이의 거리를 미터로 계산한다.

## content·quest 외에 수정한 파일과 이유

| 파일 | 변경 이유 |
|---|---|
| `user/repository/UserRepository.java` | 원래 비어 있었다. 퀘스트 진행 기록에 연결할 사용자를 ID로 조회하기 위해 `JpaRepository<User, Long>`로 완성했다. |
| `travel/repository/PlaceRepository.java` | 퀘스트 단계에 DB의 실제 장소를 연결하기 위해 추가했다. |
| `recommendation/repository/RecommendationSessionRepository.java` | 추천 세션을 조회해 퀘스트로 전환하기 위해 추가했다. |
| `recommendation/repository/SessionPlaceRepository.java` | `is_chosen=true`인 선택 장소를 노출 순서대로 읽기 위해 추가했다. |
| `recommendation/domain/RecommendationSession.java` | 퀘스트 생성 후 추천 상태를 `CONVERTED`로 바꾸는 `convertToQuest()`만 추가했다. 기존 필드와 생성자는 유지했다. |

위 파일 이외의 다른 담당 패키지는 수정하지 않았다.

## 요청 흐름

### 곡 분석

1. `POST /api/songs/{songId}/analyses`로 분석 작업 생성
2. `POST /api/analyses/{analysisId}/start`로 분석 시작
3. AI나 외부 작업 결과를 `POST /api/analyses/{analysisId}/complete`로 저장
4. `GET /api/analyses/{analysisId}/elements`로 요소 조회

현재 코드는 AI 모델을 직접 호출하지 않는다. AI 결과를 DB에 저장하고 조회하는 백엔드 구조다. 실제 AI API가 정해지면 `start`와 `complete` 사이에 호출 모듈을 연결한다.

### 퀘스트

1. `POST /api/quests`로 운영 퀘스트 생성 또는 `POST /api/quests/from-session`으로 추천 결과를 퀘스트로 변환
2. `POST /api/quests/{questId}/start?userId=1`로 시작
3. `POST /api/quests/{questId}/steps/{stepId}/arrival?userId=1`로 GPS 도착 확인
4. `POST /api/quests/{questId}/steps/{stepId}/complete?userId=1`로 미션 완료
5. 완료되면 스탬프 저장과 다음 단계 해제가 같은 트랜잭션에서 처리됨

로그인 기능이 아직 완성되지 않았기 때문에 테스트 단계에서는 `userId`를 파라미터로 받는다. 로그인 담당 코드가 완성되면 클라이언트가 보낸 userId 대신 로그인 세션/JWT에서 사용자 ID를 가져오도록 바꿔야 한다.

사진 미션은 현재 `photoUrl`이 입력됐는지만 확인한다. 실제 파일 업로드와 사진 인증은 프론트/스토리지 기능이 정해진 뒤 연결한다.

## 실행 확인

Java 21, Gradle 9.7.1에서 다음 명령으로 컴파일했다.

```powershell
.\gradlew.bat compileJava
```

결과: `BUILD SUCCESSFUL`

MySQL에는 기존 V1 스키마를 그대로 사용한다. DB 구조를 변경하지 않았기 때문에 새 Flyway 파일은 추가하지 않았다.
